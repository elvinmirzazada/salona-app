
services:
  web:
    build: .
    container_name: react_app
    expose:
      - "80"
    ports:
      - "3000:80"
    environment:
      - PORT=80
    networks:
      - app-network

networks:
  app-network:
    external: true